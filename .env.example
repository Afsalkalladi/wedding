# Where guest messages are sent. See README → "Messages from guests".
VITE_MESSAGE_ENDPOINT=

# The address the site is published at (no trailing slash). Needed for the
# WhatsApp / social link preview image. See README → "Link preview".
VITE_SITE_URL=
